``result.graph()`` now renders via ``chart.js`` instead of ``react-vis`` in the web report.

Add a ``Timeline`` graph type to ``result.graph()``. ``driver_info`` now uses it to display driver setup/teardown timings, replacing the previous ``plotly`` chart. See :ref:`here <graph_visualisation>` for more details.
