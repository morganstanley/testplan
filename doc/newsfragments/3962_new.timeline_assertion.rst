Add ``result.timeline()`` to display a Gantt-style timeline chart via ``chart.js``; ``driver_info`` now uses it to display driver setup/teardown timings. See :ref:`here <timeline_visualisation>` for more details.